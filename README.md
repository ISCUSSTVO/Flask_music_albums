🎵 MusicCatalog
MusicCatalog — это веб-приложение на Flask для создания личной коллекции музыки. Сервис позволяет управлять базой артистов и альбомов, оставлять рецензии и выставлять рейтинги, которые автоматически пересчитываются на стороне базы данных.

✨ Основные функции
👤 Для пользователей:
Каталог артистов и альбомов: Просмотр сетки альбомов с фильтрацией по жанрам.

Динамический рейтинг: Сортировка альбомов по лучшим/худшим оценкам (расчет через SQLAlchemy func.avg).

Рецензии: Возможность оставлять текстовые отзывы и ставить оценки (от 1 до 5 звезд).

Умная навигация: Кликабельные связи между артистами и их дискографией, кнопка возврата.

🛡 Для администраторов (после входа):
Управление контентом: Добавление новых артистов и альбомов через единую форму с загрузкой изображений.

Автоматическое переименование: Система сама называет файлы изображений по ID (artist_1.jpg, album_5.jpg), предотвращая битые ссылки.

Удаление: Каскадное удаление (удаление артиста автоматически удаляет все его альбомы и их обложки с диска).

🛠 Технологический стек
Backend: Python 3.x, Flask

Database: SQLAlchemy (SQLite по умолчанию)

Auth: Flask-Login

Frontend: Jinja2 Templates, CSS3 (Modern Dark Theme), JavaScript (dynamic forms)

Image Handling: Werkzeug (secure_filename)

🚀 Быстрый старт
1. Подготовка окружения
Bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/music-catalog.git
cd music-catalog

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Для Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

2. Структура папок
Убедитесь, что у вас созданы папки для медиафайлов:

Bash
mkdir -p static/img/artists
mkdir -p static/img/covers
3. Запуск
Bash
python app.py
Приложение будет доступно по адресу: http://127.0.0.1:5000

📂 Структура проекта
Plaintext
├── models/
│   └── models.py          # Описание таблиц User, Artist, Album, Review
├── static/
│   ├── css/style.css      # Стили темной темы и кнопок
│   └── img/               # Папки для загруженных обложек и фото
├── templates/
│   ├── base.html          # Глобальный шаблон с навигацией
│   ├── index.html         # Главная страница (сетка альбомов)
│   ├── artist.html        # Страница артиста и его дискография
│   ├── album.html         # Страница альбома и рецензии
│   └── add_content.html   # Админ-панель добавления
├── app.py                 # Инициализация приложения
└── media_controller.py    # Blueprint для управления контентом и удаления
⚙️ Технические особенности реализации
Рейтинг: В отличие от простых свойств Python, рейтинг считается в SQL через func.coalesce(func.avg(Review.rating), 0), что позволяет эффективно сортировать тысячи записей.

Cascade Delete: При удалении артиста используется правило cascade="all, delete-orphan", что гарантирует чистоту базы данных.

Error Handling: Для всех изображений реализован JS-обработчик onerror, подставляющий default.jpeg, если файл на сервере отсутствует.

📝 Лицензия
Проект создан в учебных целях. Свободен для использования и модификации.
